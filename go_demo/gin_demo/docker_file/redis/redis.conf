# 绑定地址（仅容器内）
bind 0.0.0.0
protected-mode no

# 监听端口
port 6379

# 设置密码
requirepass myStrongPassword

# 数据持久化
appendonly yes
appendfsync everysec

# 内存最大 100MB，超出后淘汰策略
maxmemory 100mb
maxmemory-policy allkeys-lru

# 日志文件（docker 内会打印到 stdout，保持空即可）
logfile ""
